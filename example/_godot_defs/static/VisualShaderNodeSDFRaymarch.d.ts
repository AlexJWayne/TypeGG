
/**
 * Casts a ray against the screen SDF (signed-distance field) and returns the distance travelled.
 *
*/
declare class VisualShaderNodeSDFRaymarch extends VisualShaderNode  {

  
/**
 * Casts a ray against the screen SDF (signed-distance field) and returns the distance travelled.
 *
*/
  new(): VisualShaderNodeSDFRaymarch; 
  static "new"(): VisualShaderNodeSDFRaymarch 





  connect<T extends SignalsOf<VisualShaderNodeSDFRaymarch>>(signal: T, method: SignalFunction<VisualShaderNodeSDFRaymarch[T]>): number;






}

