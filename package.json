{
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "build": "npm run clean && tsc",
    "clean": "rm -rf ./build",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@vitest/ui": "^1.3.1",
    "prettier": "^3.2.5",
    "vitest": "^1.3.1"
  },
  "dependencies": {
    "typescript": "^5.4.2"
  }
}
