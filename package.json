{
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "build": "npm run clean && tsc --project tsconfig.build.json",
    "clean": "rm -rf ./build",
    "format": "prettier --write ."
  },
  "type": "module",
  "devDependencies": {
    "@vitest/ui": "^1.3.1",
    "prettier": "^3.2.5",
    "vitest": "^1.3.1"
  },
  "dependencies": {
    "ts-morph": "^22.0.0",
    "typescript": "^5.4.2"
  }
}
